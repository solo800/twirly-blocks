/* eslint-disable no-unused-vars */
import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App/App.jsx';

ReactDOM.render(
    <App greeting='I am your React app, nice to meet you?' />,
    document.getElementById('root')
);
