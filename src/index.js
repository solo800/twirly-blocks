import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App.jsx';

ReactDOM.render(<App greeting='Hello from react land, we are the champions'/>, document.getElementById('root'));
